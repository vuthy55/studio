rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // By default, deny all reads and writes.
    // You should add specific rules to allow access where needed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
