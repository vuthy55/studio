rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This rule is insecure and for development only.
    // It allows any client to read and write to the entire database.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
